//! `crystalline-index` is the derived, disposable index built from Engram
//! files on disk. It will hold the `Store` trait, the embedded database
//! implementation, the sync engine that keeps the database in step with
//! the files, the search planner and the local embedding pipeline. It is
//! empty in this milestone; storage and search land in a later milestone.
