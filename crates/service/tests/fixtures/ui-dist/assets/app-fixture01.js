// A stand-in for one content-hashed chunk of the Fluid bundle. The name
// carries a fake hash on purpose: the immutable cache policy is keyed to the
// /assets/ prefix, not to the contents.
export const marker = "fixture-script-marker";
