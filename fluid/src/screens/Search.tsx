/**
 * Search.
 *
 * A placeholder: the topbar already routes here, and the facets and the
 * URL-driven query arrive with the task that builds them.
 */

export default function Search() {
  return <h1 className="text-xl font-semibold">Search</h1>;
}
