/**
 * The home screen.
 *
 * A placeholder: the shell needs something to render at `/`, and the domain
 * cards and the activity feed arrive with the task that builds them.
 */

export default function Home() {
  return <h1 className="text-xl font-semibold">Home</h1>;
}
