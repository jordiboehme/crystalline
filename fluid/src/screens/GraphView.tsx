/**
 * The neighborhood graph.
 *
 * A placeholder: the route exists so links can be written against it, and the
 * graph itself arrives with the task that builds it.
 */

export default function GraphView() {
  return <h1 className="text-xl font-semibold">Graph</h1>;
}
